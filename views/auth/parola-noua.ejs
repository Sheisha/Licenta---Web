<%- include('../includes/inceput.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>

<body>
   <%- include('../includes/navigare.ejs') %>

    <main>
        <% if (errorMessage) { %>
            <div class="user-message user-message--error"><%= errorMessage %></div>
        <% } %>
        <form class="login-form" action="/parola-noua" method="POST" novalidate>
            
            <div class="form-control">
                <label for="password">Password</label>
                <input 
                    type="password" 
                    name="password" id="password">
            </div>
            <input type="hidden" name="userId" value="<%= userId %>">
            <input type="hidden" name="parolaToken" value="<%= parolaToken %>">

            <button class="btn" type="submit">Modifica Parola</button>
        </form>
    </main>
<%- include('../includes/sfarsit.ejs') %>